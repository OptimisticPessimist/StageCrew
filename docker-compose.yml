services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: stagecrew
      POSTGRES_PASSWORD: stagecrew
      POSTGRES_DB: stagecrew
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
